function NameCallback(app, plate_num)


  app.plates(plate_num).filter_rows
  app.plates(plate_num).filter_columns
  app.plates(plate_num).filter_fields
  app.plates(plate_num).filter_timepoints

  app.plates(plate_num).filter_rows


  
end